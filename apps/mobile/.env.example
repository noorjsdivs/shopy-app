# Mobile API base URL. Leave unset to use the per-platform default in src/lib/env.ts
# (iOS sim: localhost, Android emulator: 10.0.2.2). For a physical device set your LAN IP:
# EXPO_PUBLIC_API_URL="http://192.168.1.50:4000/api"
